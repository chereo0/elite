# Optional: Google OAuth client id for Google Identity Services
VITE_GOOGLE_CLIENT_ID=your_google_client_id

# Optional: API base URL (only if you wire it up in the code)
# VITE_API_URL=http://localhost:5000/api
